{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "npm",
            "script": "esbuild",
            "group": "build",
            "problemMatcher": [],
            "label": "npm: esbuild",
            "detail": "esbuild `find src -name '*.ts'` --sourcemap --platform=node --target=node10 --format=cjs --outdir=dist"
        }
    ]
}